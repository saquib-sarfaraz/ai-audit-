# AI Prompts Used

**UI Generation:**
- "Generate a modern SaaS pricing section using Tailwind CSS. Use a dark, sleek aesthetic similar to Vercel."
- "Create a responsive form layout for adding multiple software tools dynamically using react-hook-form and Zod."

**Debugging Help:**
- "The backend requires `toolId`, `planId`, and `monthlySpendUsd`. The frontend is sending `name`, `plan`, `monthlySpend`. Write a mapping layer inside my onSubmit handler to fix the payload without breaking the UI."
- "My frontend throws a TypeError when trying to parse `t.name.toLowerCase()`. The data from the backend has `toolId`. How do I safely update the mapping in `report.tsx`?"

**TypeScript Fixes:**
- "Generate strict TypeScript interfaces for my API payload based on this backend Mongoose schema."
